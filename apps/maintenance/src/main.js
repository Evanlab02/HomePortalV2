import './style.css';
import { createIcons, Settings, Info, Clock, ShieldCheck, Zap, Sparkles } from 'lucide';

// Initialize Lucide icons
createIcons({
  icons: {
    Settings,
    Info,
    Clock,
    ShieldCheck,
    Zap,
    Sparkles,
  },
});
