name: hp

services:
  maintenance:
    container_name: maintenance
    restart: unless-stopped
    image: ghcr.io/evanlab02/hp2-caddy:0.0.3  # x-release-please-version
    command: "run --config /etc/caddy/maintenance/Caddyfile --adapter caddyfile"
    ports:
      - 80:80
    volumes:
      - caddy-data:/data
    networks:
      - hp-network

volumes:
  caddy-data:
    external: false

networks:
  hp-network:
    driver: bridge
