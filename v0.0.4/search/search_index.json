{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Home Portal V2","text":"<p>My updated home lab setup to account for all the things I did not think of when doing it the first time.</p>"},{"location":"#pre-requisites","title":"Pre-requisites","text":"<ul> <li>Docker</li> <li>Docker Compose</li> <li>Jellyfin</li> <li>acme.sh</li> </ul> <p>NOTE: Please ensure you read the acme.sh documentation as this will tell you how to setup certs manually. This is done as this project is run on a tailscale network and I trust people are running on some private network/vpn like service so automatic certificate management probably does not cut it.</p>"},{"location":"#installation-process","title":"Installation Process","text":"<p>Feel free to fork the repo and then clone it to your local as you might want to make some changes and adjust it to your liking as this is really probably best for a starting point for you to setup your home server.</p> <p>Otherwise just clone the repo as is and ensure to git pull every now and then for the latest updates.</p> <pre><code>git clone git@github.com:Evanlab02/HomePortalV2.git\n# OR\ngit clone https://github.com/Evanlab02/HomePortalV2.git\n# OR\ngh repo clone Evanlab02/HomePortalV2\n</code></pre> <p>NOTE: Home Portal V2 is still very early in development, so use with caution and pull with caution</p> <p>To start the compose project, you can use the following commands:</p> <pre><code>docker compose -f compose.prod.yaml pull\ndocker compose -f compose.prod.yaml build\n# You can run just the below command, the previous commands are just good to run after each pull to ensure everything is up to date.\ndocker compose -f compose.prod.yaml up \n</code></pre>"},{"location":"#enabling-maintenance-mode","title":"Enabling Maintenance Mode","text":"<p>Doing some updates, fixes or just breaking the server? Got you covered, run the following command which will prevent people from accessing the server and display a page indicating the server is not available right now:</p> <pre><code>make maintenance\n</code></pre> <p>NOTE: This is still runnning through docker so if you are updating something related to docker that causes your containers to stop well this page will not work, just keep that in mind.</p>"},{"location":"roadmap/","title":"No Release Set","text":"<ul> <li>[ ] Django Management Interface/Application</li> <li>[ ] Cloudflare API Integration (Why: Allow admins from the dedicated home portal app to change DNS records without having an enterprise/group team)<ul> <li>[ ] API</li> <li>[ ] Frontend</li> </ul> </li> <li>[ ] Authentik API Integration (Applications/Providers/Outposts) (Why: Allow admins from the dedicated home portal app to add applications)<ul> <li>[ ] API</li> <li>[ ] Frontend</li> </ul> </li> <li>[ ] Private IP/LAN IP Reminders</li> <li>[ ] Scheduled Reminders (Should be part of notifications)<ul> <li>[ ] Backups</li> </ul> </li> <li>[ ] Probs way more authentik integrations</li> </ul>"},{"location":"dependencies/acme/","title":"Acme","text":""},{"location":"dependencies/acme/#trusted-tls-certificates-for-internal-use","title":"Trusted TLS certificates for internal use","text":"<p>This is how I run all of my selfhosted services at home through my reverse proxy with trusted TLS certificates.</p> <p>This question comes up a lot on r/selfhosted so thought of sharing how I do it.</p> <p>This guide assumes you are the root user.</p>"},{"location":"dependencies/acme/#prerequisites","title":"Prerequisites","text":"<ul> <li>Domain name.</li> <li>Ability to modify DNS records for the domain name.</li> <li>Caddy (or your preferred reverse proxy) installed.</li> <li>(Optional) - for the paranoid - Your existing custom private DNS solution (like AdGuard Home, pihole, NextDNS, ControlD, etc).</li> </ul>"},{"location":"dependencies/acme/#setting-up-acmesh-and-getting-a-certificate","title":"Setting up acme.sh and getting a certificate","text":"<p>This great open source tool lets you obtain TLS certificates signed by a trusted CA like Let\u2019s Encrypt or ZeroSSL using the ACME protocol without opening ports or giving full access to your entire DNS zone. You simply add a CNAME (or more) once and this tool will keep your certificate(s) updated. This is possible using the acme-dns integration in acme.sh.</p>"},{"location":"dependencies/acme/#install","title":"Install","text":"<p>I prefer installing acme.sh to a custom path so I can give the caddy group (which caddy runs as by default) access to the /certs directory.</p> <pre><code>git clone https://github.com/acmesh-official/acme.sh.git\ncd ./acme.sh\nmkdir /certs\nmkdir /opt/acme.sh\n./acme.sh --install --cert-home /certs --home /opt/acme.sh\ncd .. &amp;&amp; rm -rf ./acme.sh\n</code></pre> <p>If you receive a warning about socat, ignore it.</p> <p>Run acme.sh --help to verify installation.</p>"},{"location":"dependencies/acme/#obtaining-a-certificate","title":"Obtaining a certificate","text":"<p>I use a single wildcard certificate for all of my internal-only service *.int.mydomain.tld.</p> <p>This way, I can have servicename.int.mydomain.tld for every service.</p> <pre><code>acme.sh --register-account -m my@example.com\nacme.sh --register-account -m my@example.com --server letsencrypt --set-default-ca\nacme.sh --issue \\\n    -k 4096 \\\n    -d \"int.mydomain.tld\" \\\n    -d \"*.int.mydomain.tld\" \\\n    --dns dns_acmedns\n</code></pre> <p>It\u2019ll tell you to add a CNAME record to your domain\u2019s DNS. You\u2019ll only have to do this once.</p> <p>Then follow the rest of the instructions.</p> <p>Certificates should be saved under /certs/int.mydomain.tld.</p>"},{"location":"dependencies/acme/#ownership-and-permissions","title":"Ownership and permissions","text":"<p>Let\u2019s set ownership and permission on this directory:</p> <pre><code>chown -R root:caddy /certs\nfind /certs -type d -exec chmod 0770 {} \\; &amp;&amp; find /certs -type f -exec chmod 0660 {} \\;\n</code></pre>"},{"location":"dependencies/acme/#dns-setup","title":"DNS setup","text":""},{"location":"dependencies/acme/#globally","title":"Globally","text":"<p>Now we need a way to resolve *.int.mydomain.tld to your local server\u2019s LAN IP address.</p> <p>If you don\u2019t run your own custom private DNS or don\u2019t want to, simply set an A record for the wildcard *.int with your local server\u2019s LAN IP address. This will resolve globally but anyone outside your network will not be able to connect.</p> <p>This does have several benefits compared to using a custom private DNS:</p> <ul> <li>Any guest connected to your LAN will be able to access your services without changing their DNS.</li> <li>If they use their own custom encrypted DNS and/or ignore your router\u2019s DNS, they will still be able to access your services, as the hostname is resolved globally.</li> </ul> <p>If your local server\u2019s IP is 192.168.1.100, it\u2019d look like this in Cloudflare.</p>"},{"location":"dependencies/acme/#on-your-custom-private-dns","title":"On your custom private DNS","text":"<p>Set the A record on your own DNS.</p>"},{"location":"dependencies/acme/#caddy-setup","title":"Caddy setup","text":"<p>Edit /etc/caddy/Caddyfile with your favorite text editor:</p> <pre><code>(inttls) {\n    tls /certs/int.mydomain.tld/fullchain.cer /certs/int.mydomain.tld/int.mydomain.tld.key\n}\n\nservice1.int.mydomain.tld {\n    import inttls\n    reverse_proxy localhost:1234\n}\n\nservice2.int.mydomain.tld {\n    import inttls\n    reverse_proxy localhost:5678\n}\n</code></pre> <p>Restart the caddy service: systemctl restart caddy</p> <p>You should now be able to access your local services through caddy with trusted TLS.</p>"},{"location":"dependencies/acme/#backups","title":"Backups","text":"<p>Simply add directories /certs and /opt/acme.sh to your backup system. As long as you have access to these, you won\u2019t have to worry about updating CNAMEs.</p>"},{"location":"dependencies/jellyfin/","title":"Jellyfin","text":"<p>Home portal relies on some services and jellyfin is one of them. Jellyfin can be set up in alot of different ways and is really the primary use of home portal for me but since it can be set up in so many different ways and there are good reasons for doing it in different ways, I am not prescriptive of how you set it up. I prefer runnnig it baremetal and not containerized so I do not have to struggle with GPUs and containers working together but there is ways to work around this but this is up to you to decide.</p>"},{"location":"dependencies/jellyfin/#setting-up-jellyfin","title":"Setting Up Jellyfin","text":"<p>Refer to the documentation @ https://jellyfin.org/docs/general/installation/</p>"}]}